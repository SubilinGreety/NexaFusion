// This directory contains static assets like SVGs, icons, and other media
// that are imported directly into React components (rather than served from public/).
//
// Usage example:
//   import logo from '@/assets/logo.svg';
//
// For large media files (images, videos), use the /public directory instead
// and reference them with absolute paths like /images/hero.webp

export {};
